# 上游同步 v0.8.0 · Ticket QA 门禁报告

> 日期：2026-09-06  
> 分支：`sync/upstream-v0.8.0` @ `d321a01c`  
> 状态：**自动化 + Route 1 + Route 2 已通过；用户确认测试后合并 sync → main**

## 一、自动化门禁（已通过）

### Go（Docker `golang:1.26` + CGO）

包：`deploydefaults` / `router` / `sandbox` / `handler` / `application/service` / `container`  
过滤：Teaching | Invitation | PlatformIdentity | Viewer | Contributor | DefaultAgent | Note | Announcement | Health Teaching | DockerBackendEnabled | StudentPublish | MyEnvVar | IM/Embed | Artifact | Attachment | EnsureTeaching  

结果：全部 `ok`

### 前端 tsx 契约

`settingsAccess` / `integrations` / `brandSeal` / `studentSeal` / `creatChat`：**21/21 pass**

### 部署默认静态核对

`docker-compose.yml`：`WEKNORA_SANDBOX_DOCKER_ENABLED=…:-false`、`DISABLE_REGISTRATION=…:-true`、`invite_only`、`tenantless`、自助建空间 `false`。

## 二、Route 1 本地空库冒烟（已通过）

项目名：`weknora-sync-qa`（独立 volume；未碰旧 `weknora_postgres-data`）  
端口：当时 `APP_PORT=8080` / `FRONTEND_PORT=8088`  
脚本：`scripts/qa_sync_smoke.py`

| # | 项 | 状态 |
|---|---|---|
| 1–2 | 建空间 / 邀请链接 / 公告 / 学生入课与封闭面 | ✅ |
| 4 | `invite_only` + sandbox 默认关 | ✅ |
| — | 登录页川农品牌 | ✅ |
| 3 | 升级迁移 | N/A（新鲜库；见 §二-B） |

## 二-B、Route 2 备份副本升级冒烟（已通过）

| 项 | 值 |
|---|---|
| 源卷 | `weknora_postgres-data`（**只读克隆**，未改源） |
| 克隆卷 | `weknora-sync-upgrade_postgres-data` |
| 项目 | `weknora-sync-upgrade` |
| API | `http://127.0.0.1:8081` |
| 脚本 | `scripts/qa_sync_upgrade_smoke.py` |

迁移：`schema_migrations` **96 → 98**（`dirty=false`）；日志：`Database migrated from version 96 to 98`；`[bootstrap] SuperAdmin invariant OK`。

冒烟（克隆库上临时重置了 **clone 内** SuperAdmin 密码为 QA 值，**未改** `weknora_postgres-data`）：

```
PASS upgrade admin login
INFO registration_mode=invite_only
PASS tenant / teacher / invite-link / announcement
PASS student register + members/env-vars sealed
PASS student notes + announcements
```

清单 3（升级路径）✅。

## 三、合并门禁（用户已确认）

规格要求：经确认后合并 `sync/upstream-v0.8.0` → `main`，不 force-push。

1. ✅ QA 修复已推送（`01fb8ef6` bootstrap/`default_agent_id`；`c5bbcceb` 冒烟脚本；`d321a01c` anydoc 0.2.4）  
2. ✅ 用户确认本机测试通过并授权合并  
3. 可选关升级栈：`COMPOSE_PROJECT_NAME=weknora-sync-upgrade docker compose down`（勿对源卷 `-v`）

操作记忆：`docs/上游同步操作记忆.md`

## 四、风险备注

- sync 相对旧 main 是整棵 v0.8.0 换基 + A–G；合并替换运行基线。  
- Host 探针需 `--noproxy "*"`。  
- `.env` 含密钥，勿提交。  
- 克隆库 QA 密码与现网无关；现网账号密码未改。

## 五、QA 发现（已合入 sync）

| 问题 | 修复提交 |
|---|---|
| 空库 + `invite_only` 无 SuperAdmin | `01fb8ef6` |
| `default_agent_id` nil → NOT NULL | `01fb8ef6` |
| 冒烟脚本契约 | `01fb8ef6` / `c5bbcceb` |
| anydoc 不可用 / 过旧 | `WITH_ANYDOC=1` + `d321a01c`（0.2.4） |
