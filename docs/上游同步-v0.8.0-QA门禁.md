# 上游同步 v0.8.0 · Ticket QA 门禁报告

> 日期：2026-09-06  
> 分支：`sync/upstream-v0.8.0` @ `efa8b10b` + **未提交** QA 修复（见 §五）  
> 状态：**自动化门禁已通过；Route 1 本地 compose API 冒烟已通过；合并仍需你确认**

## 一、自动化门禁（已通过）

### Go（Docker `golang:1.26` + CGO）

包：`deploydefaults` / `router` / `sandbox` / `handler` / `application/service` / `container`  
过滤：Teaching | Invitation | PlatformIdentity | Viewer | Contributor | DefaultAgent | Note | Announcement | Health Teaching | DockerBackendEnabled | StudentPublish | MyEnvVar | IM/Embed | Artifact | Attachment | EnsureTeaching  

结果：全部 `ok`

覆盖规格清单映射：

| QA 项 | 自动化证据 |
|---|---|
| 教师邀请 / viewer-only / 成员 Admin+ | Invitation / tenant member / teaching migration 测试 |
| 平台身份 / SuperAdmin 教师能力 | PlatformIdentity / AppointTeacher 相关 |
| 学生纯问答（附件/产物/默认 Agent） | Contributor gates + DefaultAgent + studentSeal |
| 笔记 / 公告 | Note / Announcement 测试 |
| 新对话标题 + 居中 | `creatChat.test.ts`（前端） |
| 川农品牌 / 无上游 GitHub 菜单回归 | `brandSeal.test.mjs` |
| 发布集成 / 沙箱密钥学生封闭 | `student_publish_seal` + settingsAccess/integrations |
| Docker 沙箱默认关 | `docker_enabled` + `deploydefaults` compose/env 契约 |
| 教学迁移 fail-closed | Ticket A container/health 测试（本轮一并跑过） |

### 前端 tsx 契约

`settingsAccess` / `integrations` / `brandSeal` / `studentSeal` / `creatChat`：**21/21 pass**

### 部署默认静态核对

`docker-compose.yml`：`WEKNORA_SANDBOX_DOCKER_ENABLED=…:-false`、`DISABLE_REGISTRATION=…:-true`、`invite_only`、`tenantless`、自助建空间 `false`。

## 二、Route 1 本地 compose 冒烟（已通过）

项目名：`weknora-sync-qa`（独立 volume；未碰旧 `weknora_postgres-data`）  
端口：`APP_PORT=8080` / `FRONTEND_PORT=8088`  
脚本：`.worktrees/sync-upstream-v0.8.0/scripts/qa_sync_smoke.py`

| # | 项 | 状态 |
|---|---|---|
| 1 | SuperAdmin 登录 + `/auth/me` | ✅ |
| 1 | 建课程空间 `POST /tenants` | ✅（经 §五 `default_agent_id` 修复后） |
| 1 | 创建教师用户 + `teachers/appoint` | ✅ |
| 1 | 共享邀请链接 `POST …/invite-links`（viewer） | ✅ |
| 1 | 发公告（multipart） | ✅ |
| 2 | `register-by-invite` 学生入课 | ✅ |
| 2 | 学生：成员名单 403、env-vars 403 | ✅ |
| 2 | 学生：笔记 / 公告可读 | ✅ |
| 2 | 学生：`GET /agents` 200（默认 Agent 列表可达） | ✅ INFO |
| 4 | `auth/config` → `invite_only`；sandbox env false | ✅（起栈时已核对） |
| — | 登录页川农品牌（`http://127.0.0.1:8088/login`） | ✅ 一眼确认 |
| — | UI「新对话居中」/ 沙箱开关 UI | ⏳ 未做浏览器逐步点检（API+契约已覆盖） |
| 3 | 升级：备份副本迁移 | ⏳ 未跑（Route 1 新鲜库） |

冒烟结论摘要（末次）：

```
PASS admin login / auth/me / tenant ready
PASS create teacher + appoint
PASS invite-link + announcement
PASS student register-by-invite + sealed members/env-vars
PASS student notes + announcements readable
```

## 三、合并门禁（未开）

规格要求：**经确认后**再合并 `sync/upstream-v0.8.0` → `main`，不自动强推。

合并前建议：

1. 在 sync 分支 **提交并推送** §五 QA 修复（否则合并后空库仍缺 SuperAdmin / 建空间仍会炸）。  
2. 你明确说「合并」后，再开 PR / 执行 merge。  
3. 可选：`COMPOSE_PROJECT_NAME=weknora-sync-qa docker compose down`（`-v` 仅在确认可丢 QA 库时加）。

## 四、风险备注

- sync 相对 main 是整棵 v0.8.0 换基 + A–G；合并会替换 main 运行基线，合并前务必备份 DB。  
- 本机 `classroom-app` (:3000) 与 WeKnora FE 已错开（8088）；起栈用独立 compose project。  
- Host 探针需 `--noproxy "*"`（否则 Privoxy 返回 500）。  
- `.env` 含密钥，勿提交。

## 五、QA 发现并已在 sync worktree 修掉（未提交）

| 问题 | 影响 | 修复 |
|---|---|---|
| 空库 + `invite_only` 无 SuperAdmin 引导 | 无法登录 | 移植 `internal/bootstrap` + `cmd/server` fail-closed + compose `WEKNORA_BOOTSTRAP_SUPERADMIN_*` |
| `POST /tenants`：`default_agent_id` NOT NULL，GORM 对 nil `*string` 写 NULL | 无法建空间 | `CreateTenant` 在入库前把 nil 归一成 `""` |
| 冒烟脚本误用邮箱邀请 / JSON 公告 | 假失败 | 改为 `invite-links` + multipart 公告 |

未提交路径（sync worktree）：

- `internal/bootstrap/*`
- `cmd/server/bootstrap.go` / `main.go`
- `docker-compose.yml`（bootstrap env）
- `internal/application/service/tenant.go`（`default_agent_id`）
- `scripts/qa_sync_smoke.py`
