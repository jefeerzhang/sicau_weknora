# 05 — 使用统计两列（提问数 + 最后活跃）

**What to build:** 教师在成员管理列表中看到每个学生的"累计提问数"和"最后活跃时间"，用于识别"谁还没用起来"（ADR-009-2）。统计只到计数与时间粒度，不含任何提问内容明细；该页在 ticket 01 完成后仅教师可见。

**Blocked by:** 01 — 成员名单仅教师可见（统计列不能在学生可见的成员页上出现）

**Status:** done

- [x] 教师成员列表新增两列：累计提问数、最后活跃时间，数据按空间内各成员聚合
- [x] 统计接口为 admin/owner 专属；学生直接调用返回 403
- [x] 无任何会话的学生显示 0 次和"从未活跃"，不显示空值或报错
- [x] 统计不包含任何消息内容、标题等明细字段（接口响应里也没有）
- [x] 大列表（百人级）加载性能可接受（聚合走一次请求，不逐人查询）
